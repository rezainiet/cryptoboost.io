

export default function RootLayout({ children }) {
    return (
        <html lang="en">
            <head>
                <style>{
                }</style>
            </head>
            <body className="font-sans antialiased">{children}</body>
        </html>
    )
}
